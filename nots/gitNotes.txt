git init
git remote add origin link https
git fetch --all     ..all brach chack krnya sathi and give
git checkout -b new-branch-name      create new branch
git checkout exit-brach-name              eka  brach madhun durya branch madhe jane

git branch -a              .....all branch list check     * means  live work in this branch

     git add .

git commit -m "description"         description mens  whatever you want
git  push origin  working-branch-name   ;ex vaishu

git pull origin other-branch-name    "" oter branch madhala data working branch madhe ghene

git log    ........all commits chack   